<div class="container px-4">
  <%= render "includes/admin_sub_nav" %>

  <div class="row">
    <div class="col-2">
      <%= render "includes/side_nav_production" %>
    </div>

    <div class="col-10">
      <% if current_user.user_group.admin_cru %>
        <a href="<%= new_admin_product_part_path %>">
          <button class="btn btn-primary">
            <%= show_svg("plus.svg") %>
            Create Product Category
          </button>
        </a>
      <% end %>

      <div class="table-responsive">
        <table class="table table-borderless table-hover">
          <thead>
            <tr>
              <th style="width: 90%">Title</th>
              <th style="width: 10%">Action</th>
            </tr>
          </thead>
          <tbody>
            <% @product_parts.each do |product_part| %>
              <tr>
                <td><%= product_part.name %></td>
                <td>
                  <% if current_user.user_group.admin_cru %>
                    <a href="<%= edit_admin_product_part_path(product_part) %>" class="mr-4">
                      <%= show_svg("recent_activity.svg") %>
                    </a>
                    <%= link_to admin_product_part_path(product_part), method: :delete, data: { confirm: "Are you sure you want to delete this part?" } do %>
                      <%= show_svg("close.svg") %>
                    <% end %>
                  <% end %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>