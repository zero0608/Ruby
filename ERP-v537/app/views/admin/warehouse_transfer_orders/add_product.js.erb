$("#productPurchase").append("<%= escape_javascript(render partial: 'products', locals: { current_user: @current_user, current_store: current_store, transfer_order: @transfer_order })%>");
$("#productPurchaseModal").modal('show');