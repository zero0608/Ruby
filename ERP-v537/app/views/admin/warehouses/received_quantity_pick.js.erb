$("#quantityPick").append("<%= escape_javascript(render partial: 'received_quantity_pick', locals: { current_user: current_user,variant: @variant, received_quantity: params[:received_quantity] })%>");
$("#quantityPickModal").modal('show');