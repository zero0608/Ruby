$("#quantityPick").append("<%= escape_javascript(render partial: 'to_do_quantity', locals: { current_user: current_user,variant: @variant, to_do_quantity: params[:to_do_quantity] })%>");
$("#quantityPickModal").modal('show');